# Background music

Put a soft romantic track here named exactly:

    romantic.mp3

If this file is missing, the site automatically plays a gentle generated
ambient piano pad instead — so it is never silent.

Keep it under ~4 MB for fast loading (a 2–3 minute loop is ideal).
